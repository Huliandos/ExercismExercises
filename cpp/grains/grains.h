#if !defined(GRAINS_H)
#define GRAINS_H

namespace grains {
    const int chessBoardSquares = 64;

    unsigned long long int square(int squareNum);
    unsigned long long int total();
}  // namespace grains

#endif // GRAINS_H